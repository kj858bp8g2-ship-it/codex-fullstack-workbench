interface:
  display_name: "Document Ingest"
  short_description: "Convert and inspect documents without context bloat"
  default_prompt: "Use $document-ingest to convert and analyze these files efficiently."
