interface:
  display_name: "Testing and Debugging"
  short_description: "Reproduce failures and verify fixes systematically"
  default_prompt: "Use $testing-debugging to reproduce this issue and verify the smallest fix."
